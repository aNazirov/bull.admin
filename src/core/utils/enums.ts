export enum RoleType {
  Admin = 1,
  Moderator,
  User,
}

export enum SlideoverModes {
  edit = "Edit",
  show = "Show",
  create = "Create",
  none = "None",
}

export enum THead {
  avatar = "Avatar",
  edit = "Edit",
}
