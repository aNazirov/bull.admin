export enum RoleType {
  Admin = 1,
  User,
}

export enum SlideoverModes {
  edit = "Edit",
  show = "Show",
  create = "Create",
  none = "None",
}

export enum THead {
  avatar = "Avatar",
  edit = "Edit",
}

export enum BannerPosition {
  full = "full",
  right = "right",
  left = "left",
}

export enum ContextPriority {
  urgent = "urgent",
  medium = "medium",
  low = "low",
}
